global:
  resolve_timeout: 5m

route:
  group_by: ["alertname"]
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: "email-notifications"

receivers:
  - name: "email-notifications"
    email_configs:
      - to: "iaconnection237@gmail.com"
        from: "iaconnection237@gmail.com"
        smarthost: smtp.gmail.com:587
        auth_username: "iaconnection237@gmail.com"
        auth_password: "hymplrqbwswxtzlj"
        headers:
          Subject: "[ALERTE] {{ .GroupLabels.alertname }}"
